export enum DataModels {
	DB = 'db',
	SCHEMA = 'schema',
	TABLE = 'base table',
	VIEW = 'view',
	COLUMN = 'column',
}

export enum Usage {
	HIGH = 'high',
	MEDIUM = 'medium',
	LOW = 'low',
	UNUSED = 'unused',
}
